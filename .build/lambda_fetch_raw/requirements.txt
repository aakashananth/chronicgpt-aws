# AWS Lambda requirements for health metrics pipeline
# Note: boto3 is pre-installed in Lambda runtime, so it's not included here

# Data validation and models
pydantic>=2.0.0
pydantic-settings>=2.0.0
typing-extensions==4.12.2

# HTTP requests
requests>=2.31.0

# Data processing
pandas>=2.0.0
numpy<2.0.0,>=1.23.2  # Pin to numpy 1.x for Lambda compatibility (numpy 2.x requires GCC 9.3+)

# Azure OpenAI
openai>=1.0.0

